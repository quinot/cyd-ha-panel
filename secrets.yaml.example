# secrets.yaml.example — copy to secrets.yaml and fill in your values.
# secrets.yaml is git-ignored and never committed.

# OTA password — used to authenticate firmware uploads.
ota_password: "changeme"
